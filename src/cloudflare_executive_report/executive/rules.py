"""Superseded by the ``rules/`` package.

Python resolves ``executive.rules`` to the ``rules/`` directory (package)
rather than this file. All logic has moved to ``rules/__init__.py`` and the
per-stream sub-modules in ``rules/``. This file is intentionally empty.
"""
